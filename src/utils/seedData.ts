// Seed data is now handled automatically by the NestJS backend.
// When a new user registers via POST /api/auth/sign-up, the backend
// automatically creates 18 default categories for that user.
//
// To seed additional data manually, use the NestJS API directly
// or run the backend's built-in seeding logic via auth registration.

export {}
